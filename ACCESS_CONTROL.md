# Access Control - Контроль доступа к боту

## Обзор

Система контроля доступа ограничивает использование бота только авторизованными пользователями по их Telegram ID. Это помогает экономить токены AI API и предотвращает несанкционированное использование.

## Зачем это нужно?

- 💰 **Экономия токенов** - AI API обычно платный, защита от нежелательных запросов
- 🔒 **Приватность** - бот доступен только вам и доверенным лицам
- 🛡️ **Безопасность** - предотвращение спама и злоупотреблений

## Настройка

### 1. Получение своего Telegram ID

Есть несколько способов узнать свой Telegram ID:

**Способ 1: Использовать @userinfobot**
1. Откройте Telegram
2. Найдите бота @userinfobot
3. Отправьте ему любое сообщение
4. Он ответит с вашим ID

**Способ 2: Использовать @raw_data_bot**
1. Найдите бота @raw_data_bot
2. Отправьте ему /start
3. В ответе найдите поле `"id": 123456789`

### 2. Добавление разрешенных пользователей

Откройте `.env` файл и добавьте/обновите строку:

```env
ALLOWED_USER_IDS=123456789,987654321,555666777
```

- Указывайте ID через **запятую**
- Пробелы игнорируются (можно указывать `123, 456` или `123,456`)
- Можно указать один или несколько ID

### 3. Режимы работы

#### Режим "Только для избранных" (рекомендуется)

Укажите конкретные ID в `.env`:

```env
ALLOWED_USER_IDS=123456789,987654321
```

Только пользователи с указанными ID смогут пользоватьсяботом.

#### Режим "Для всех" (по умолчанию)

Оставьте переменную пустой или удалите её:

```env
# ALLOWED_USER_IDS=
```

или просто удалите строку. Бот будет доступен всем.

## Как это работает

### Архитектура

```
User Message
     ↓
AccessControlMiddleware ← Проверка ID
     ↓
[Доступ разрешен?]
  ├─ Да → Обработчик → AI Response
  └─ Нет → Отказ в доступе
```

### Middleware

Middleware перехватывает **все** входящие сообщения до их обработки:

1. Получает ID отправителя
2. Проверяет его в списке разрешенных
3. Если ID найден → пропускает дальше
4. Если ID не найден → отправляет сообщение об отказе

### Файлы

```
middlewares/
├── __init__.py           # Экспорт middleware
└── access_control.py     # Логика контроля доступа
```

## Сообщения

### Для неавторизованных пользователей

Если пользователь не в списке, он получит:

```
🚫 Извини, у тебя нет доступа к этому боту.

Бот работает только для авторизованных пользователей.
```

### Логирование

При неправильном формате `ALLOWED_USER_IDS` в консоль выводится:

```
Warning: Invalid ALLOWED_USER_IDS format in .env file
```

## Примеры использования

### Пример 1: Личный бот

Только для вас:

```env
ALLOWED_USER_IDS=123456789
```

### Пример 2: Семейный бот

Для вас и членов семьи:

```env
ALLOWED_USER_IDS=123456789,987654321,555666777
```

### Пример 3: Командный бот

Для рабочей команды:

```env
ALLOWED_USER_IDS=111222333,444555666,777888999,100200300
```

### Пример 4: Открытый бот

Доступен всем (не рекомендуется с AI):

```env
# Просто не указывайте ALLOWED_USER_IDS, либо оставьте пустым
ALLOWED_USER_IDS=
```

## Получение ID других пользователей

Чтобы добавить кого-то в whitelist:

1. Попросите человека написать @userinfobot в Telegram
2. Он получит свой ID
3. Попросите его прислать вам этот ID
4. Добавьте ID в `.env` через запятую

## Безопасность

- ✅ ID пользователей хранятся в `.env` (не коммитится в git)
- ✅ Проверка происходит до любой обработки
- ✅ Middleware работает на уровне всего диспетчера
- ✅ Невозможно обойти проверку

## Troubleshooting

### "Бот не отвечает после добавления middleware"

Проверьте формат `ALLOWED_USER_IDS`:
- Используйте только цифры и запятые
- Убедитесь, что ваш ID указан правильно
- Проверьте `.env` файл на ошибки

### "Как узнать, заработал ли контроль доступа?"

1. Запустите бота
2. Отправьте сообщение со своего аккаунта (ID должен быть в списке)
3. Попросите друга отправить сообщение (его ID НЕ в списке)
4. Друг должен получить сообщение об отказе

### "Можно ли динамически менять список?"

Сейчас список загружается при старте бота. Чтобы обновить:
1. Отредактируйте `.env` файл
2. Перезапустите бота

## Расширенная настройка

### Кастомизация сообщения об отказе

В файле `middlewares/access_control.py` найдите:

```python
await event.answer(
    "🚫 Извини, у тебя нет доступа к этому боту.\n\n"
    "Бот работает только для авторизованных пользователей."
)
```

Измените текст на свой.

### Логирование попыток доступа

Можно добавить логирование неудачных попыток:

```python
else:
    # Логируем попытку доступа
    print(f"Access denied for user {event.from_user.id} ({event.from_user.username})")
    
    await event.answer(...)
```

### Отключение контроля доступа

Если нужно временно отключить:

1. **Способ 1**: Удалите/закомментируйте `ALLOWED_USER_IDS` в `.env`
2. **Способ 2**: Закомментируйте регистрацию middleware в `bot.py`:

```python
# dp.message.middleware(AccessControlMiddleware())
```

## Best Practices

1. 🔐 **Всегда используйте контроль доступа** с AI-ботами
2. 📝 **Документируйте** список авторизованных пользователей
3. 🔄 **Регулярно проверяйте** кто в списке
4. 🗑️ **Удаляйте** ID пользователей, которым больше не нужен доступ
5. ⚠️ **Не делитесь** своим `.env` файлом
6. 💾 **Делайте бэкапы** списка разрешенных ID

## Совместимость

Middleware совместим со всеми обработчиками:
- ✅ Команды (/start, /help)
- ✅ Текстовые сообщения (AI)
- ✅ Админские команды
- ✅ FSM (машины состояний)
- ✅ Callback queries (при расширении middleware)

## Альтернативы

Если контроль по ID не подходит, можно реализовать:

- 👤 Проверка по username
- 🔑 Система паролей/токенов
- 📅 Временные доступы
- 💳 Подписочная модель
- 🎫 Пригласительные коды

Но контроль по ID - самый простой и надежный способ!
